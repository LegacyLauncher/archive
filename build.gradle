plugins {
    id "java"
    id "org.ajoberstar.grgit" version "4.0.2" apply false
}

allprojects {
    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
    repositories {
        jcenter()
        mavenCentral()
    }
}
